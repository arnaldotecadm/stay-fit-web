import React from 'react';
import PlaceholderPage from '../../components/PlaceholderPage';

const SettingsPage: React.FC = () => (
  <PlaceholderPage icon="settings" title="Settings" />
);

export default SettingsPage;
